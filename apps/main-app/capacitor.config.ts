import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
   appId: 'io.ionic.starter',
   appName: 'main-app',
   webDir: '../../dist/apps/main-app',
   bundledWebRuntime: false,
};

export default config;
